# 강준모의 소개팅 — 설계·개발기

> AI로 소개팅 게임을 만들었다. 근데 "AI한테 프롬프트 던졌더니 게임이 나왔다"는 얘기가 아니라,
> **캐릭터 심리부터 설계하고 → 대사를 뽑아내고 → 감독처럼 검수한** 기록이다.
> 플레이: https://junmo-game.vercel.app · 코드/설계: https://github.com/codethisai/Junmo-game

---

## 0. 세 줄 요약

- 29살 모태솔로 **강준모**가 세 히로인과 5단계 소개팅을 하는 **한국어 AI 비주얼노벨**.
- 대사는 LLM이 즉흥으로 뱉는 게 아니라, **손으로 설계한 11턴 대본 + LLM 실시간 변주**의 하이브리드.
- 1인 개발, Claude Code로 바이브코딩. 근데 재미의 8할은 **AI가 아니라 캐릭터 설계**에서 나왔다.

---

## 1. 문제의식 — "AI 소개팅 게임"은 왜 대부분 재미없나

LLM한테 "너는 소개팅 상대야, 대화해"라고 시키면 그럴듯한 첫 문장은 나온다. 근데 3턴만 지나면:

- **캐릭터가 붕괴한다.** 츤데레였다가 갑자기 헤퍼지고, 조용한 애가 수다스러워진다.
- **맥락이 증발한다.** 아까 한 말을 기억 못 하거나, 아무 데서나 사랑 고백을 한다.
- **긴장이 없다.** 뭘 해도 받아주니 "공략"이라는 게임성이 사라진다.

즉 **순수 LLM 자유대화 = 캐릭터·서사·게임성이 다 무너지는 구조.** 그래서 방향을 반대로 잡았다:

> **대화의 본진은 "설계된 대본". LLM은 그 위의 보조 레이어.**

각 히로인 × 스테이지마다 **11턴짜리 대본**을 손으로 짜고, 선택지마다 호감도 증감·캐릭터 반응을 못박았다. 플레이어가 자유입력하면 그때만 LLM이 캐릭터 설정을 받아 실시간으로 받아친다. 뼈대는 설계, 살은 AI.

---

## 2. 캐릭터 설계 — '대사'가 아니라 '사람'부터

대사를 먼저 쓰면 캐릭터가 흔들린다. 그래서 **심리 뼈대(욕망·두려움·방어기제)**부터 박고, 거기서 대사를 연역했다. 세 명을 **정반대 축**으로 세운 게 핵심:

| | 🌸 김유정 (ENFP) | 📚 박지은 (INFJ) | 🎤 이수아 (ESFP) |
|---|---|---|---|
| 한 줄 | 밝고 먼저 다가감 | 조용한 관찰자 | 화려한 가면 |
| **욕망** | 편하게 잘 통하는 대화 | 깊고 진솔한 연결 | 관심·재미·텐션 |
| **두려움** | 건성·나 혼자 떠드는 침묵 | 무방비하게 **읽혀** 상처 | 지루해져 **잊히는** 것 |
| **방어기제** | 분위기 띄우고 먼저 말 검 | 먼저 읽어서 거리 잼 | 텐션·콘텐츠화로 공허 덮기 |
| **지뢰(식는 것)** | 폰·건성·자기 얘기만 | 아는 척·진부한 질문 | 폰만 봄·억지·자기세계 |

> **대비 설계 한 줄**: 수아 = *보여지고 싶은데 외로운* / 지은 = *안 보여지고 싶은데 읽히는*. 완전히 반대편에 세워야 셋이 안 겹친다.

이 시트를 옵시디언 볼트(`design-vault/`)로 관리했다. `01_캐릭터`(심리)·`02_스테이지`(장소·정서 곡선)·`04_대사·씬`(생성 템플릿)·`05_검수`(채점 리포트). 코드보다 이 문서가 먼저였다.

관계 아크도 캐릭터마다 다르게:
- **유정** = 정통 연애 progression. 첫만남 → 한강 → 사귐 → 동거 → 프러포즈 (5스테가 2년 반을 관통).
- **지은** = 관찰자가 무너지는 이야기. S4 「읽히는 자의 공포」에서 *눈물 폭발이 아니라 '간파당한 정지의 순간'*(동공 흔들림·숨 멈춤)으로 무너진다.
- **수아** = 가면 벗기. S3(집)에서 "화면 속 나만 좋아하잖아"를 고백하는 게 감정 절정.

---

## 3. 대사 파이프라인 — 볼트 → 템플릿 → 대본 → 검수

캐릭터 시트가 있으면, 대본은 **공장처럼** 뽑는다.

1. **생성 템플릿** — 캐릭터 core·목소리·지뢰 + 스테이지 장소·정서 곡선을 슬롯에 끼워 11턴 대본 생성.
2. **6대 작성 규칙** (실제 플레이 검수에서 도출한 강제 규칙):
   - **준모 자동대사 금지** — 주인공 대사를 response에 박으면 플레이어 페르소나와 충돌. 준모 목소리는 선택지로만.
   - **속마음 = 감정/서브텍스트** — 결론·캐릭터 선언 금지. "가면이 있구나" (X) → 대사가 안 해준 감정만.
   - **한 박자로** — 한 response에 화자 셋 욱여넣지 않기.
   - **선택지 = 직전 대사 받기** — 동문서답 금지.
   - **명대사 페이싱** — 깊은 통찰은 신뢰 쌓인 후반(T7+)에. 초반은 워밍업.
   - **메아리 금지** — 속마음이 직전 대사를 요약하지 않기.
3. **감독 채점 검수** — 생성된 대사를 어색함 1~5점으로 채점하고, 위반 규칙·수정안을 리포트로 남김(`05_검수/`).

실제로 초기 버전은 첫 만남 2턴 만에 "당신은 침묵으로 사람을 보는군요" 같은 **완성형 잠언**을 뱉었다. 첫 만남에 저러면 가짜다. 검수 → 규칙6(페이싱) 위반 → "무던하게" 재작성. 이런 before/after가 스테이지마다 쌓여 있다.

---

## 4. 게임 시스템 — 호감도가 표정·씬·엔딩을 움직인다

- **호감도(affinity)**: 선택지마다 증감. 지뢰 선택지는 감점. 구간별로 게임이 반응한다.
- **표정 슬롯**: `호감도 ≥70 → smile / 35~69 → neutral / <35 → bored`. 같은 캐릭터라도 호감도 따라 다른 누끼 이미지가 뜬다.
- **씬 분기**: `getSceneKey(partner, stage, affinity)`가 배경+캐릭터를 고른다.
- **엔딩 판정**: 스테이지 끝 호감도로 great/good/friend/crush/fail 분기. 잘하면 다음 스테로, 못하면 관계 종료.
- **히든 엔딩·도전과제**: 전 스테 great, 10턴 내 클리어 등 수집 요소.

### 호감도 페이싱 — "슬로우번" 재설계
초기엔 선택지 하나에 +10~20씩 붙어서, **2턴 만에 완전설렘(호감도 92)**에 도달했다. 클리어가 85인데 시작이 50이니, 잘 찍으면 순식간에 끝. 긴장이 0.

→ 전 스테이지 델타를 **재스케일**했다. 시작 50에서 **베스트 플레이로도 T11(마지막 턴)에 겨우 85에 닿게**. 결과: 관심있음(70)은 T6~7에, 완전설렘/클리어(85)는 막판에. "한 턴 한 턴이 아까운" 슬로우번이 됐다.

---

## 5. 비주얼 파이프라인 — 1GB 서버에서 누끼 뜨기

캐릭터는 배경 제거(누끼)한 webp 오버레이다. 서버가 **RAM 1GB**라 제약이 많았다.

- **rembg `isnet-anime`** 모델을 Python API로 호출(CLI는 filetype 이슈로 실패). 애니풍 세그멘테이션이라 흰 셔츠도 인물로 잘 잡는다.
- **검은 배경 / 흰 체커보드 배경** 원본 둘 다 처리. (스톡 이미지의 체커보드가 픽셀로 박혀 오는 함정도 있었다.)
- 메모리 방어: **입력 1000px 축소** + `OMP_NUM_THREADS=2` + 프로세스 단발 실행(끝나면 RAM 반환). available <500Mi면 사전 경고.
- **그린 디스필**: 원본 림라이트의 초록 잔흔을 `G = max(R,B)`로 눌러 제거.
- `cwebp -q 90`(캐릭터, 알파 유지) / `-q 80`(배경). 원본 대비 1/5~1/10.

장당 ~50초, 한 번에 하나씩. 스웨트지만 무료 서버로 여기까지 왔다.

---

## 6. AI로 만든다는 것 — 바이브코딩의 실제

Claude Code로 만들었다는 게 "AI가 알아서 다 했다"는 뜻은 아니다. **판단·설계·검수는 사람, 실행·리팩터·삽질은 AI**에 가까웠다. 실제 버그 몇 개:

- **스테이지가 1에서 안 넘어감** — 대본은 11턴인데 종료 조건이 `MAX_TURNS(20)`뿐. 11턴 다 쓰면 선택지가 비고, 20턴 전이라 종료도 안 됨 → 교착. **스크립트 소진을 종료 조건에 추가**해서 해결.
- **엔딩에 엉뚱한 캐릭터** — 세 파트너가 전부 옛 base64 이미지(유정)를 가리켜서, 지은·수아 엔딩에도 유정이 떴다. **파트너별 엔딩 이미지 분리**.
- **표정이 안 뜸** — S5 씬이 `smile/surprised/emotional` 키를 쓰는데, 호감도 로직은 `smile/neutral/bored`만 조회 → 폴백. **슬롯 키 정합**.

이런 걸 **커밋 단위로 기록**하고 `BUGS.md`에 증상·원인·수정을 남겼다. AI로 빨리 짜는 만큼, 기록을 안 하면 뭘 왜 고쳤는지 순식간에 증발한다.

---

## 7. 스택 & 배포

| 구분 | 선택 | 이유 |
|---|---|---|
| 프론트 | React (CRA) | 단순·빠른 시작 |
| AI | Groq `llama-3.3-70b` | 무료·빠른 추론 |
| 배포 | Vercel | GitHub 푸시 자동 배포 |
| 이미지 | rembg + cwebp | 로컬 무료 파이프라인 |
| 저장 | localStorage | 백엔드 0원 유지 |

지금까지 **AI 비용 0원, 인프라 0원.** (확산되면 API 키 서버사이드 보호 + 유료 한도로 전환 예정.)

---

## 8. 회고 & 다음

- **배운 것**: AI로 만드는 게임의 승부처는 프롬프트가 아니라 **설계와 검수**다. "사람부터 설계"한 캐릭터가 결국 재미를 만든다.
- **다음**: 백엔드(키 보호·세이브 동기화) → 콘텐츠 확장 → 그리고 **성장(홍보)을 개발과 동급 기둥으로** 관리 중([`GROWTH.md`](GROWTH.md)).

읽어주셔서 감사합니다. 플레이해보고 어떤 히로인이 제일 취향인지, 어디서 어색했는지 알려주시면 반영합니다.

- 플레이: https://junmo-game.vercel.app
- 코드·설계 볼트: https://github.com/codethisai/Junmo-game

---

*설계·개발기 · 2026-07 · 아카라이브/velog/GitHub 공용 원천 문서*
