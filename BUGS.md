# 강준모의 소개팅 — 버그 트래커

> 모든 버그는 여기에 기록. TODO.md에 버그 중복 기록 금지.
> 상태: 🔲 Open | 🔄 In Progress | ✅ Fixed

---

## ✅ 스테이지 1에서 진행 멈춤 — 발견: 2026-06-30

**증상**: 유정·지은·수아 모두 스테이지 1에서 다음 스테로 안 넘어가고 멈춤.
**근본 원인**: 스크립트는 11턴인데 종료 조건이 `MAX_TURNS(20)`뿐이라, 11번째 턴 선택 후 `script.turns[11]`(없음)을 못 불러 선택지가 비고, 20턴 전이라 스테이지도 안 끝남. 입력창은 `turnsLeft<=7`에만 떠서 11~12턴 구간엔 선택지·입력 둘 다 없는 교착.
**수정**: `sendScripted` 종료 조건에 **스크립트 소진(`!script.turns[newTurn]`)** 추가 → 대본이 끝나면 스테이지 종료 → 엔딩 판정 → 다음 스테 진행. (`src/screens/GameScreen.jsx`)
**상태**: ✅ Fixed

---

## 현재 보고된 버그 없음 (위 외)

버그 발생 시 아래 형식으로 추가:

```
## 🔲 [버그 제목] — 발견: YYYY-MM-DD

**증상**: 어떻게 나타나는가
**재현 방법**: 단계별 재현 방법
**근본 원인**: 왜 발생하는가 (진단 후 작성)
**수정**: 어떻게 고쳤는가
**PR**: #번호
**상태**: 🔲 Open → ✅ Fixed
```
